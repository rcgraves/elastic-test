# Author Ross Petty
# Date 10/19/15
# Rev 1
output {
  if "firewall" in [tags] {
#    stdout { codec => rubydebug }
    elasticsearch {
      hosts => [ "172.16.1.8", "172.16.1.9" ]
      index => "logstash-firewall-%{+YYYY.MM.dd}"
    }
  }
}
